const ViewProductsButton = ({label, onClick}) => {
    return (
        <button 
            className=""
            onClick={onClick}
        >
            {label}
        </button>
    )
}

export default ViewProductsButton;