import DashboardPlaceholderView from "../../components/Dashboard/Views/DashboardPlaceholderView";

const SettingsPage = () => {
    return (
        <DashboardPlaceholderView
            title="Settings"
            description="Settings now has a dedicated page route. You can split account, preferences, and team settings into sub-sections here."
        />
    );
};

export default SettingsPage;
