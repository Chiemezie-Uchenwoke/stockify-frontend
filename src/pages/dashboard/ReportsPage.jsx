import DashboardPlaceholderView from "../../components/Dashboard/Views/DashboardPlaceholderView";

const ReportsPage = () => {
    return (
        <DashboardPlaceholderView
            title="Reports"
            description="Reports now lives on its own page route. Add charts and exports here without adding conditional UI branches."
        />
    );
};

export default ReportsPage;
