import DashboardPlaceholderView from "../../components/Dashboard/Views/DashboardPlaceholderView";

const SalesPage = () => {
    return (
        <DashboardPlaceholderView
            title="Sales"
            description="Sales reporting now has its own page route. You can grow this page independently without touching the rest of the dashboard."
        />
    );
};

export default SalesPage;
